import std.io.stdout;
import std.range;
import std;

func main() -> i32 {
    print("Test");
    
    loop(true) {
        print("Test2" + "test");
        break;
    }
}

// u8, u16, u32, u64, i8, i16, i32, i64, f32, f64, etc.
// dynamic array built in for interpreter
// Strings are dynamic arrays of chars
// chars are u32s and guarenteed to be valid UTF-8